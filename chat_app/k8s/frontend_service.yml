kind: Service
apiVersion: v1
metadata:
  name: frontend-service
  namespace: chat-app
spec:
  type: NodePort
  selector:
    app: frontend
  ports:
  - name: http
    port: 80
    targetPort: 5173
    nodePort: 30007
